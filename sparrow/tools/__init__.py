"""Optional batteries — not part of the pure core.

A host that only needs the engine can ignore this package entirely. It holds the
tool registry, the built-in tool battery, the restricted-expression engine, and
the panels-as-memory helpers.
"""
